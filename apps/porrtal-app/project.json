{
  "root": "apps/porrtal-app",
  "sourceRoot": "apps/porrtal-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/porrtal-app",
        "outputPath": "dist/apps/porrtal-app"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "porrtal-app:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "porrtal-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "porrtal-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/porrtal-app"],
      "options": {
        "jestConfig": "apps/porrtal-app/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/porrtal-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
